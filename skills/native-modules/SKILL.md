---
name: native-modules
description: "Gunakan untuk native modules, Expo modules, Turbo Modules, native linking, config plugins. Trigger: native module, Expo module, Turbo Module, native code, Swift, Kotlin, config plugin, eject."
---
# Native Modules — Expo Modules + Turbo Modules
## Expo SDK first: cek expo.dev/modules sebelum buat custom
## Config plugins: app.config.ts plugins untuk native config tanpa eject
## Expo Modules API: Swift/Kotlin wrapper dengan TypeScript bindings
## Development builds: `npx expo prebuild` + `npx expo run:ios`
